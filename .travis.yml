language: c++
dist: bionic
compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get install -y scons
  - sudo apt-get install -y libboost-all-dev

script:
  - scons
  - scons debug=1
  - scons boost=1
